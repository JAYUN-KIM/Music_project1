[analysis_node]
# localhost를 대상으로 하며, 접속 방식(connection)을 local로 설정합니다.
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3

[analysis_node:vars]
# 로컬 테스트이므로 SSH 관련 설정은 모두 생략하거나 무시됩니다.
# 배포 시 사용할 사용자 계정만 현재 로그인한 사용자로 맞춰줍니다.
ansible_user={{ lookup('env', 'USER') }}
